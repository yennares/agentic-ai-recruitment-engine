"""Specialist agents for the Candidate Evaluation Pipeline.

Each module owns exactly one responsibility and returns a structured dict
(or, for the two on-demand generation agents, formatted text). orchestrator.py
sequences them; nothing here calls another agent module directly.
"""
